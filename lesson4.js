const myObj = {
    type: 'dog',
    name: 'arjuk',
    age:  '2',
    hasFam 'true'
};

function isHappy(valod) {
    if (valod.hasFam === true) {
return valod.name + 'is happy to have a family';
      } else {
return 'Please help find a family for' + valod.type + ' ' + valod.name;
}
};

console.log(isHappy(myObj));